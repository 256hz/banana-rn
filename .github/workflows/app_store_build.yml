on:
  push:
    branches:
      - CI/CD_Setup
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        variant: [client, donor]
        platform: [ios] # Include android if needed

    steps:
      - name: 🏗 Setup repo
        uses: actions/checkout@v4

      - name: 🏗 Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18.x
          cache: npm

      - name: 📝 Configure Git safe directory
        run: git config --global --add safe.directory $GITHUB_WORKSPACE

      - name: 🏗 Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 📦 Install dependencies
        run: npm install

      - name: Debug Environment Variables
        run: |
          echo "EXPO_PUBLIC_APP_VARIANT=${{ matrix.variant }}"
          echo "EXPO_USE_DEFAULT_LOCATION=true"
          echo "ENVIRONMENT_MODE=production"
          echo "GOOGLE_MAPS_API_KEY=${{ secrets.GOOGLE_MAPS_API_KEY }}"
          echo "EXPO_IP_ADDRESS=${{ secrets.EXPO_IP_ADDRESS }}"
          echo "EXPO_STORYBOOK=false"
          if [ "${{ matrix.variant }}" == "client" ]; then
            echo "EAS_BUILD_PROFILE=client-production"
            echo "EAS_PROJECT_ID=${{ secrets.CLIENT_APP_ID }}"
          fi
          if [ "${{ matrix.variant }}" == "donor" ]; then
            echo "EAS_BUILD_PROFILE=donor-production"
            echo "EAS_PROJECT_ID=${{ secrets.DONOR_APP_ID }}"
          fi

      - name: Set environment variables
        run: |
          echo "EXPO_PUBLIC_APP_VARIANT=${{ matrix.variant }}" >> $GITHUB_ENV
          echo "EXPO_USE_DEFAULT_LOCATION=true" >> $GITHUB_ENV
          echo "ENVIRONMENT_MODE=production" >> $GITHUB_ENV
          echo "EXPO_STORYBOOK=false" >> $GITHUB_ENV
          if [ "${{ matrix.variant }}" == "client" ]; then
            echo "EAS_BUILD_PROFILE=client-production" >> $GITHUB_ENV
            echo "CLIENT_APP_ID=${{ secrets.CLIENT_APP_ID }}" >> $GITHUB_ENV
          fi
          if [ "${{ matrix.variant }}" == "donor" ]; then
            echo "EAS_BUILD_PROFILE=donor-production" >> $GITHUB_ENV
            echo "DONOR_APP_ID=${{ secrets.DONOR_APP_ID }}" >> $GITHUB_ENV
          fi

      - name: 🚀 Build app
        run: eas build --platform ${{ matrix.platform }} --profile ${{ env.EAS_BUILD_PROFILE }} --non-interactive
